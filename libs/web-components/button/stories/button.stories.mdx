import { Meta, Story, Canvas, ArgsTable, Props } from '@storybook/addon-docs';
import { html } from 'lit';
import { ifDefined } from 'lit/directives/if-defined.js';

import '@finastra/button';

<Meta
  title="Components/Button"
  component={'fds-button'}
  argTypes={{
    label: {
      table: {
        category: 'properties',
        defaultValue: { summary: 'fds button' }
      },
      control: 'text',
      defaultValue: 'fds button',
      type: 'string',
      description: 'button label'
    },
    icon: {
      table: {
        category: 'properties',
        defaultValue: { summary: 'search' }
      },
      control: 'text',
      defaultValue: 'search',
      type: 'string',
      description: 'button icon'
    },
    secondary: {
      table: {
        category: 'attributes',
        defaultValue: { summary: false }
      },
      control: 'boolean',
      defaultValue: false,
      type: 'boolean',
      description: 'Secondary button'
    },
    gradient: {
      table: {
        category: 'attributes',
        defaultValue: { summary: false }
      },
      control: 'boolean',
      defaultValue: false,
      type: 'boolean',
      description: 'Gradient button'
    },
    outlined: {
      table: {
        category: 'attributes',
        defaultValue: { summary: false }
      },
      control: 'boolean',
      defaultValue: false,
      type: 'boolean',
      description: 'Outlined button'
    },
    text: {
      table: {
        category: 'attributes',
        defaultValue: { summary: false }
      },
      control: 'boolean',
      defaultValue: false,
      type: 'boolean',
      description: 'Text button'
    },
    dense: {
      table: {
        category: 'attributes',
        defaultValue: { summary: false }
      },
      control: 'boolean',
      defaultValue: false,
      type: 'boolean',
      description: 'Dense button'
    },
    large: {
      table: {
        category: 'attributes',
        defaultValue: { summary: false }
      },
      control: 'boolean',
      defaultValue: false,
      type: 'boolean',
      description: 'Large button'
    },
    disabled: {
      table: {
        category: 'attributes',
        defaultValue: { summary: false }
      },
      control: 'boolean',
      defaultValue: false,
      type: 'boolean',
      description: 'Disabled button'
    },
    fullwidth: {
      table: {
        category: 'attributes',
        defaultValue: { summary: false }
      },
      control: 'boolean',
      defaultValue: false,
      type: 'boolean',
      description: 'Full Width button'
    }
  }}
/>

export const Template = ({ icon, label, outlined, text, dense, large, disabled, secondary = false, gradient = false, fullwidth = false }) => {
  return html`<fds-button
    .label=${label}
    .icon=${ifDefined(icon)}
    ?secondary=${secondary}
    ?gradient=${gradient}
    ?outlined=${outlined}
    ?text=${text}
    ?dense=${dense}
    ?large=${large}
    ?disabled=${disabled}
    ?fullwidth=${fullwidth}
  ></fds-button>`;
};

# fds-button

<Canvas>
  <Story
    name="Raised"
    args={{ icon: 'favorite', label: 'Raised button' }}
    parameters={{
      docs: {
        source: {
          code: '<fds-button label="Raised button" icon="favorite"></fds-button>'
        }
      }
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Installation

```
npm install @finastra/fds-button

```

## API

<ArgsTable of="." story="^" />

## Examples

### Secondary button

<Canvas>
  <Story
    name="Secondary"
    args={{ secondary: true, label: 'Secondary button', icon: 'favorite' }}
    parameters={{
      docs: {
        source: {
          code: '<fds-button secondary label="Secondary button" icon="favorite"></fds-button>'
        }
      }
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Gradient button

<Canvas>
  <Story
    name="Gradient"
    args={{ gradient: true, label: 'Gradient button', icon: 'favorite' }}
    parameters={{
      docs: {
        source: {
          code: '<fds-button gradient label="Gradient button" icon="favorite"></fds-button>'
        }
      }
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Outlined button

<Canvas>
  <Story
    name="Outlined"
    args={{ icon: 'favorite', label: 'Button outlined', outlined: 'true' }}
    parameters={{
      docs: {
        source: {
          code: '<fds-button outlined label="Button outlined" icon="favorite"></fds-button>'
        }
      }
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### text button

<Canvas>
  <Story
    name="Text"
    args={{ icon: 'favorite', label: 'Button text', text: 'true' }}
    parameters={{
      docs: {
        source: {
          code: '<fds-button text label="Button text" icon="favorite"></fds-button>'
        }
      }
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### dense button

<Canvas>
  <Story
    name="Dense"
    args={{ icon: 'favorite', label: 'Button dense', dense: 'true' }}
    parameters={{
      docs: {
        source: {
          code: '<fds-button dense label="Button dense" icon="favorite"></fds-button>'
        }
      }
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### large button

<Canvas>
  <Story
    name="Large"
    args={{ icon: 'favorite', label: 'Button large', large: 'true' }}
    parameters={{
      docs: {
        source: {
          code: '<fds-button large label="Button dense" icon="favorite"></fds-button>'
        }
      }
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### disabled button

<Canvas>
  <Story
    name="Disabled"
    args={{ icon: 'favorite', label: 'Button disabled', disabled: 'true' }}
    parameters={{
      docs: {
        source: {
          code: '<fds-button disabled label="Button disabled" icon="favorite"></fds-button>'
        }
      }
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Full Width button

<Canvas>
  <Story
    name="Full Width"
    args={{ icon: 'favorite', label: 'Full width button', fullwidth: 'true' }}
    parameters={{
      docs: {
        source: {
          code: '<fds-button fullwidth label="Full width button" icon="favorite"></fds-button>'
        }
      }
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

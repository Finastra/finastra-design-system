import { Meta, Story, Canvas, ArgsTable, Props } from '@web/storybook-prebuilt/addon-docs/blocks.js';
import { html } from 'lit';

import '@finastra/logo';
import '@material/mwc-drawer';
import '@material/mwc-list';

<Meta
  title="Components/Sidenav"
/>

export const Template = ({ dense = false }) => {
  return html`<style>
    .fds-sidenav {
      --mdc-drawer-width: 300px;
      background-color: var(--fds-surface);
      color: var(--fds-on-surface);
    }
    .fds-sidenav-header {
      background-color: var(--fds-background);
      height: 210px;
      display: flex;
      justify-content: center;
      align-items: center;
    }
    .fds-sidenav-list {
      width: 100%;
    }
  </style>
  <mwc-drawer>
    <div class="fds-sidenav">
      <div class="fds-sidenav-header">
        <fds-logo></fds-logo>
      </div>
      <div class="fds-sidenav-list">
        <mwc-list activatable>
          <mwc-list-item selected activated>Home</mwc-list-item>
          <mwc-list-item>Applications</mwc-list-item>
          <mwc-list-item>Tools</mwc-list-item>
          <mwc-list-item>Settings</mwc-list-item>
        </mwc-list>
      </div>
    </div>
    <div slot="appContent" class="app">
    </div>
  </mwc-drawer>`;
};

# Sidenav

<Canvas>
  <Story
    name="Default"
    parameters={{
      docs: {
        source: {
          code: '<mwc-drawer></mwc-drawer>'
        }
      }
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Installation

```
npm install @material/mwc-drawer

```

## API

<ArgsTable of="." story="^" />


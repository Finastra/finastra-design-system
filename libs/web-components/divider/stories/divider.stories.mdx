import { Meta, Story, Canvas, ArgsTable, Props } from '@web/storybook-prebuilt/addon-docs/blocks.js';
import { html } from 'lit';
import { ifDefined } from 'lit/directives/if-defined.js';

import '@finastra/divider';

<Meta
  title="Components/Divider"
  component={'fds-divider'}
  argTypes={{
    vertical: {
      table: {
        category: 'attributes',
        defaultValue: { summary: false }
      },
      control: 'boolean',
      defaultValue: false,
      type: 'boolean',
      description: 'Vertically oriented'
    },
    inset: {
      table: {
        category: 'attributes'
      },
      control: 'string',
      type: 'string',
      description: 'Inset divider, accepts value : both | left | right '
    },
    '-fds-divider-inset-margin': {
      table: {
        category: 'css',
        defaultValue: { summary: '80px' }
      },
      type: 'string',
      description: 'inset margin value',
      control: null
    }
  }}
/>

export const Template = ({ inset = undefined, vertical = false }) => {
  return html`<fds-divider style='flex: 1; height: 50px;' inset=${ifDefined(inset)} ?vertical=${vertical}></fsd-divider>`;
};

# Divider

Divider component displays a line separator with various orientation options.

<Canvas>
  <Story
    name="Default"
    parameters={{
      docs: {
        source: {
          code: '<fds-divider></fds-divider>'
        }
      }
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Installation

```
npm install @finastra/divider

```

## API

<ArgsTable of="." story="^" />

## Examples

### Inset left

<Canvas>
  <Story
    name="Inset left"
    args={{ inset: 'left' }}
    parameters={{
      docs: {
        source: {
          code: `<fds-divider inset='left'></fds-Divider>`
        }
      }
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Inset right

<Canvas>
  <Story
    name="Inset right"
    args={{ inset: 'right' }}
    parameters={{
      docs: {
        source: {
          code: `<fds-divider inset='right'></fds-Divider>`
        }
      }
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Inset both

<Canvas>
  <Story
    name="Inset both"
    args={{ inset: 'both' }}
    parameters={{
      docs: {
        source: {
          code: `<fds-divider inset='both'></fds-Divider>`
        }
      }
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Vertical

<Canvas>
  <Story
    name="Vertical"
    args={{ vertical: 'true' }}
    parameters={{
      docs: {
        source: {
          code: `<fds-divider vertical></fds-Divider>`
        }
      }
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

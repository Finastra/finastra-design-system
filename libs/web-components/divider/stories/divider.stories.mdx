import { Meta, Story, Canvas, ArgsTable, Props } from '@web/storybook-prebuilt/addon-docs/blocks.js';
import { html } from 'lit-html';
import { ifDefined } from 'lit-html/directives/if-defined';

import '@finastra/divider';

<Meta
  title="Divider"
  component={'fds-divider'}
  argTypes={{
    vertical: {
      table: {
        category: 'attributes',
        defaultValue: { summary: false },
      },
      control: 'boolean',
      defaultValue: false,
      type: 'boolean',
      description: 'Vertically oriented',
    },
    inset: {
      table: {
        category: 'attributes',
      },
      control: 'string',
      type: 'string',
      description: 'Inset divider, accepts value : both | left | right ',
    },
    '-fds-divider-inset-margin': {
      table: {
        category: 'css',
        defaultValue: { summary: '80px' },
      },
      type: 'string',
      description: 'inset margin value',
      control: null,
    },
    '--fds-divider-color': {
      table: {
        category: 'css',
        defaultValue: { summary: 'rgba(0, 0, 0, 0.12)' },
      },
      type: 'string',
      description: 'Divider color',
      control: null,
    },
  }}
/>

export const Template = ({ inset = undefined, vertical = false }) => {
  return html`<fds-divider style="flex: 1" inset=${ifDefined(inset)} ?vertical=${vertical}></fsd-divider>`;
};

# Divider

Divider component displays a line separator with various orientation options.

<Canvas>
  <Story
    name="Default"
    parameters={{
      docs: {
        source: {
          code: '<fds-divider></fds-divider>',
        },
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Installation

```
npm install @finastra/divider

```

## API

<ArgsTable of="." story="^" />

## Examples

### Inset

<Canvas>
  <Story
    name="Inset"
    args={{ inset: 'both' }}
    parameters={{
      docs: {
        source: {
          code: '<fds-divider inset="both"></fds-Divider>',
        },
      },
    }}
  >
    {Template({ inset: 'both' })}
  </Story>
</Canvas>

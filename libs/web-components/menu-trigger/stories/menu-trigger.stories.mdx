import { Meta, Story, Canvas, ArgsTable, Props } from '@storybook/addon-docs';
import { action } from '@storybook/addon-actions';
import { html } from 'lit';

import '@finastra/menu-trigger';

<Meta
  title="Components/Menu Trigger"
  component={'fds-menu-trigger'}
  argTypes={{
    label: {
      table: {
        category: 'properties',
        defaultValue: { summary: 'Launch' }
      },
      control: 'text',
      defaultValue: 'Launch',
      type: 'string',
      description: 'Button label'
    },
    on: {
      table: {
        category: 'attributes',
        defaultValue: { summary: false }
      },
      control: 'boolean',
      defaultValue: false,
      type: 'boolean',
      description: 'Menu Trigger state'
    },
    secondary: {
      table: {
        category: 'attributes',
        defaultValue: { summary: false }
      },
      control: 'boolean',
      defaultValue: false,
      type: 'boolean',
      description: 'secondary button'
    },
    outlined: {
      table: {
        category: 'attributes',
        defaultValue: { summary: true }
      },
      control: 'boolean',
      defaultValue: true,
      type: 'boolean',
      description: 'Outlined button'
    },
    toggle: {
      action: 'toggle',
      description: 'Toggle function to open/close the menu'
    }
  }}
/>

export const Template = ({label, on, secondary = false, outlined}) => {
  return html`<fds-menu-trigger .label=${label} ?on=${on} ?secondary=${secondary} ?outlined=${outlined}></fds-menu-trigger>`;
};

# menu-trigger

<Canvas>
  <Story
    name="Default"
    parameters={{
      docs: {
        source: {
          code: '<fds-menu-trigger></fds-menu-trigger>'
        }
      }
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Installation

```
npm install @finastra/menu-trigger

```

## API

<ArgsTable of="." story="^" />

## Examples

### Custom

<Canvas>
  <Story
    name="Custom"
    args={{ label: 'Design', secondary: 'true', outlined: 'true'}}
    parameters={{
      docs: {
        source: {
          code: '<fds-menu-trigger></fds-menu-trigger>'
        }
      }
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

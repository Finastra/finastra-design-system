import { Meta, Story, Canvas, ArgsTable, Props } from '@web/storybook-prebuilt/addon-docs/blocks.js';
import { html } from 'lit';

import '@finastra/base-card';

<Meta
  title="Components/Base Card"
  component={'fds-base-card'}
  argTypes={{
    outlined: {
      table: {
        category: 'attributes',
        defaultValue: { summary: '' }
      },
      control: 'boolean',
      defaultValue: false,
      type: 'boolean',
      description: ''
    }
  }}
/>

export const Template = ({ outlined = false }) => {
  return html`<fds-base-card ?outlined=${outlined} style="height: 100px;
    width: 200px;"></fsd-base-card>`;
};

# base-card

<Canvas>
  <Story
    name="Default"
    parameters={{
      docs: {
        source: {
          code: '<fds-base-card></fds-base-card>'
        }
      }
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Installation

```
npm install @finastra/base-card

```

## API

<ArgsTable of="." story="^" />

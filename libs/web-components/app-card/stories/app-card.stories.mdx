import { Meta, Story, Canvas, ArgsTable, Props } from '@web/storybook-prebuilt/addon-docs/blocks.js';
import { html } from 'lit';

import '@finastra/app-card';

<Meta
  title="Components/App Card"
  component={'fds-app-card'}
  argTypes={{
    label: {
      table: {
        category: 'properties',
        defaultValue: { summary: '' }
      },
      control: 'text',
      defaultValue: 'Application',
      type: 'string',
      description: 'app card label'
    },
    shortLabel: {
      table: {
        category: 'properties',
        defaultValue: { summary: '' }
      },
      control: 'text',
      defaultValue: 'APP',
      type: 'string',
      description: 'optional app card displayed label'
    },
    secondary: {
      table: {
        category: 'attributes',
        defaultValue: { summary: false }
      },
      control: 'boolean',
      defaultValue: false,
      type: 'boolean',
      description: 'color and ribbon type'
    },
    large: {
      table: {
        category: 'attributes',
        defaultValue: { summary: false }
      },
      control: 'boolean',
      defaultValue: false,
      type: 'boolean',
      description: 'card size'
    },
    dense: {
      table: {
        category: 'attributes',
        defaultValue: { summary: false }
      },
      control: 'boolean',
      defaultValue: false,
      type: 'boolean',
      description: 'card size'
    },
    extraDense: {
      table: {
        category: 'attributes',
        defaultValue: { summary: false }
      },
      control: 'boolean',
      defaultValue: false,
      type: 'boolean',
      description: 'card size'
    }
  }}
/>

export const Template = ({ secondary = false, large = false, extraDense = false, dense = false, label, shortLabel }) => {
  return html`<fds-app-card
    ?secondary=${secondary}
    ?large=${large}
    ?extraDense=${extraDense}
    ?dense=${dense}
    .label=${label}
    .shortLabel=${shortLabel}
  ></fds-app-card>`;
};

# Default App Card

<Canvas>
  <Story
    name="Default"
    args={{ label: 'App', dense: true }}
    parameters={{
      docs: {
        source: {
          code: `<fds-app-card label="App" dense></fds-app-card>`
        }
      }
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Installation

```
npm install @finastra/app-card

```

## API

<ArgsTable of="." story="^" />

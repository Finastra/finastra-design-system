import { Meta, Story, Canvas, ArgsTable, Props } from '@storybook/addon-docs';
import { html } from 'lit';

import '@finastra/app-card';

<Meta
  title="Components/App Card"
  component={'fds-app-card'}
  argTypes={{
    label: {
      table: {
        category: 'properties',
        defaultValue: { summary: '' }
      },
      control: 'text',
      defaultValue: 'Application',
      type: 'string',
      description: 'app card label'
    },
    shortLabel: {
      table: {
        category: 'properties',
        defaultValue: { summary: '' }
      },
      control: 'text',
      defaultValue: '',
      type: 'string',
      description: 'optional app card displayed label'
    },
    secondary: {
      table: {
        category: 'attributes',
        defaultValue: { summary: false }
      },
      control: 'boolean',
      defaultValue: false,
      type: 'boolean',
      description: 'color and ribbon type'
    },
    large: {
      table: {
        category: 'attributes',
        defaultValue: { summary: false }
      },
      control: 'boolean',
      defaultValue: false,
      type: 'boolean',
      description: 'card size'
    },
    dense: {
      table: {
        category: 'attributes',
        defaultValue: { summary: false }
      },
      control: 'boolean',
      defaultValue: false,
      type: 'boolean',
      description: 'card size'
    },
    extraDense: {
      table: {
        category: 'attributes',
        defaultValue: { summary: false }
      },
      control: 'boolean',
      defaultValue: false,
      type: 'boolean',
      description: 'card size'
    }
  }}
/>

export const Template = ({ secondary = false, large = false, extraDense = false, dense = false, label, shortLabel }) => {
  return html`<fds-app-card
    ?secondary=${secondary}
    ?large=${large}
    ?extraDense=${extraDense}
    ?dense=${dense}
    .label=${label}
    .shortLabel=${shortLabel}
  ></fds-app-card>`;
};

# Default App Card

<Canvas>
  <Story
    name="Default"
    args={{ label: 'App' }}
    parameters={{
      docs: {
        source: {
          code: `<fds-app-card label="App"></fds-app-card>`
        }
      }
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Installation

```
npm install @finastra/app-card

```

## API

<ArgsTable of="." story="^" />

# Secondary

<Canvas>
  <Story
    name="Secondary"
    args={{ label: 'Application', secondary: true }}
    parameters={{
      docs: {
        source: {
          code: `<fds-app-card label="App" secondary></fds-app-card>`
        }
      }
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

# Short Label

<Canvas>
  <Story
    name="Short Label"
    args={{ label: 'Application', shortLabel: 'APP' }}
    parameters={{
      docs: {
        source: {
          code: `<fds-app-card label="App" shortLabel="A"></fds-app-card>`
        }
      }
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

# Large

<Canvas>
  <Story
    name="Large"
    args={{ large: true }}
    parameters={{
      docs: {
        source: {
          code: `<fds-app-card large></fds-app-card>`
        }
      }
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

# Dense

<Canvas>
  <Story
    name="Dense"
    args={{ dense: true }}
    parameters={{
      docs: {
        source: {
          code: `<fds-app-card dense></fds-app-card>`
        }
      }
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

# Extra Dense

<Canvas>
  <Story
    name="Extra Dense"
    args={{ extraDense: true }}
    parameters={{
      docs: {
        source: {
          code: `<fds-app-card extraDense></fds-app-card>`
        }
      }
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

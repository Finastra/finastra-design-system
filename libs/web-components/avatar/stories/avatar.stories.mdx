import { Meta, Story, Canvas, ArgsTable, Props } from '@web/storybook-prebuilt/addon-docs/blocks.js';
import { html } from 'lit';

import '@finastra/avatar';

<Meta
  title="Components/Avatar"
  component={'fds-avatar'}
  argTypes={{
    name: {
      table: {
        category: 'attributes',
        defaultValue: { summary: '' }
      },
      control: 'text',
      defaultValue: '',
      type: 'text',
      description: 'Avatar name'
    },
    shortName: {
      table: {
        category: 'attributes',
        defaultValue: { summary: '' }
      },
      control: 'text',
      defaultValue: '',
      type: 'text',
      description: 'Avatar short name'
    },
    dense: {
      table: {
        category: 'attributes',
        defaultValue: { summary: 'false' }
      },
      control: 'boolean',
      defaultValue: false,
      type: 'boolean',
      description: 'Dense avatar'
    },
    large: {
      table: {
        category: 'attributes',
        defaultValue: { summary: 'false' }
      },
      control: 'boolean',
      defaultValue: false,
      type: 'boolean',
      description: 'Large avatar'
    },
    primary: {
      table: {
        category: 'attributes',
        defaultValue: { summary: 'false' }
      },
      control: 'boolean',
      defaultValue: false,
      type: 'boolean',
      description: 'Primary avatar'
    },
    secondary: {
      table: {
        category: 'attributes',
        defaultValue: { summary: 'false' }
      },
      control: 'boolean',
      defaultValue: false,
      type: 'boolean',
      description: 'Secondary avatar'
    }
  }}
/>

export const Template = ({ name, shortName, dense=false, large=false, primary=false, secondary=false }) => {
  return html`<fds-avatar
    name=${name}
    shortName=${shortName}
    ?dense=${dense}
    ?large=${large}
    ?primary=${primary}
    ?secondary=${secondary}
  ></fds-avatar>`;
};

# Default Avatar

<Canvas>
  <Story
    name="Default"
    args={{ name: "" , large:false }}
    parameters={{
      docs: {
        source: {
          code: '<fds-avatar></fds-avatar>'
        }
      }
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Installation

```
npm install @finastra/avatar

```

## API

<ArgsTable of="." story="^" />

## Examples

### Large avatar

<Canvas>
  <Story
    name="Large"
    args={{ large: true }}
    parameters={{
      docs: {
        source: {
          code: '<fds-avatar large></fds-avatar>'
        }
      }
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Dense avatar

<Canvas>
  <Story
    name="Dense"
    args={{ dense: true }}
    parameters={{
      docs: {
        source: {
          code: '<fds-avatar dense></fds-avatar>'
        }
      }
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Colored avatar

<Canvas>
  <Story
    name="Primary"
    args={{ primary: true }}
    parameters={{
      docs: {
        source: {
          code: '<fds-avatar primary></fds-avatar>'
        }
      }
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Avatar with letter

<Canvas>
  <Story
    name="Letter"
    args={{ name: "Raya"}}
    parameters={{
      docs: {
        source: {
          code: '<fds-avatar name="Raya"></fds-avatar>'
        }
      }
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

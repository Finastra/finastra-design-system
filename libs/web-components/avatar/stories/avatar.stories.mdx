import { Meta, Story, Canvas, ArgsTable, Props } from '@web/storybook-prebuilt/addon-docs/blocks.js';
import { html } from 'lit';

import '@finastra/avatar';

<Meta
  title="Components/Avatar"
  component={'fds-avatar'}
  argTypes={{
    name: {
      table: {
        category: 'attributes',
        defaultValue: { summary: '' }
      },
      control: 'text',
      defaultValue: '',
      type: 'text',
      description: 'Somebody'
    },
    '--fds-avatar-text-color': {
      table: {
        category: 'css',
        defaultValue: { summary: 'Text color' }
      },
      type: 'string',
      description: 'Text color',
      control: null
    }
  }}
/>

export const Template = ({ name = '' }) => {
  return html`<fds-avatar  .name=${name}></fsd-avatar>`;
};

# avatar

<Canvas>
  <Story
    name="Default"
    parameters={{
      docs: {
        source: {
          code: '<fds-avatar></fds-avatar>'
        }
      }
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Installation

```
npm install @finastra/avatar

```

## API

<ArgsTable of="." story="^" />

## Examples

### Name

<Canvas>
  <Story
    name="Name"
    parameters={{
      docs: {
        source: {
          code: '<fds-avatar name="Workd"></fds-avatar>'
        }
      }
    }}
  >
    {Template.bind({}, 'World')}
  </Story>
</Canvas>

import { Meta, Story, Canvas, ArgsTable, Props } from '@web/storybook-prebuilt/addon-docs/blocks.js';
import { ifDefined } from 'lit-html/directives/if-defined';

import { html } from 'lit-html';

import '@finastra/autocomplete';

<Meta
  title="Components/Autocomplete"
  component={'fds-autocomplete'}
  argTypes={{
    icon: {
      table: {
        category: 'properties',
        defaultValue: { summary: '' }
      },
      control: 'text',
      defaultValue: '',
      type: 'string',
      description: 'search icon'
    },
    showClearButton: {
      table: {
        category: 'properties',
        defaultValue: { summary: false }
      },
      control: 'boolean',
      defaultValue: false,
      type: 'boolean',
      description: 'show clear button'
    },
    value: {
      table: {
        category: 'properties',
        defaultValue: { summary: '' }
      },
      control: 'text',
      defaultValue: '',
      type: 'string',
      description: 'value of component'
    },
    minLengthToOpenMenu: {
      table: {
        category: 'properties',
        defaultValue: { summary: 0 }
      },
      control: 'number',
      defaultValue: 0,
      type: 'number',
      description: ''
    },
    required: {
      table: {
        category: 'properties',
        defaultValue: { summary: false }
      },
      control: 'boolean',
      defaultValue: false,
      type: 'boolean',
      description: 'required'
    },
    disabled: {
      table: {
        category: 'properties',
        defaultValue: { summary: false }
      },
      control: 'boolean',
      defaultValue: false,
      type: 'boolean',
      description: 'disabled'
    },
    useInnerFilter: {
      table: {
        category: 'properties',
        defaultValue: { summary: true }
      },
      control: 'boolean',
      defaultValue: true,
      type: 'boolean',
      description: 'use local filter'
    },
  }}
/>

export const Template = ({ icon, showClearButton, value, minLengthToOpenMenu=0, required, disabled, useInnerFilter }) => {
  return html`<fds-autocomplete .value=${ifDefined(value)}  .required=${ifDefined(required)}  .disabled=${ifDefined(disabled)} .minLengthToOpenMenu=${ifDefined(minLengthToOpenMenu)} .icon=${ifDefined(icon)} .showClearButton=${ifDefined(showClearButton)}>
     <mwc-list-item value="One">One</mwc-list-item>
    <mwc-list-item value="Two">Two</mwc-list-item>
    <mwc-list-item value="Three">Three</mwc-list-item>
  </fsd-autocomplete>`;
};

# autocomplete

<Canvas>
  <Story
    name="Default"
    parameters={{
      docs: {
        source: {
          code: '<fds-autocomplete></fds-autocomplete>'
        }
      }
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Installation

```
npm install @finastra/autocomplete

```

## API

<ArgsTable of="." story="^" />

## Examples

import { Meta, Story, Canvas, ArgsTable, Props } from '@web/storybook-prebuilt/addon-docs/blocks.js';
import { html } from 'lit-html';
import { ifDefined } from 'lit-html/directives/if-defined';

import '@finastra/card';

import '@material/mwc-button';

<Meta
  title="Components/Card"
  component={'fds-card'}
  argTypes={{
    outlined: {
      table: {
        category: 'attributes',
        defaultValue: { summary: false }
      },
      control: 'boolean',
      defaultValue: false,
      type: 'boolean',
      description: ''
    },
    fullBleed: {
      table: {
        category: 'attributes',
        defaultValue: { summary: false }
      },
      control: 'boolean',
      defaultValue: false,
      type: 'boolean',
      description: ''
    },
    'icon-action': {
      table: {
        category: 'slot'
      },
      type: 'string',
      description: '',
      control: null
    },
    'button-action': {
      table: {
        category: 'slot'
      },
      type: 'string',
      description: '',
      control: null
    },
    'primary-action': {
      table: {
        category: 'slot'
      },
      type: 'string',
      description: '',
      control: null
    }
  }}
/>

export const Template = ({ outlined = false, fullBleed = false }) => {
  return html` <style>
      .card__header {
        display: flex;
        flex-direction: row;
        padding: 16px;
      }
      .card__header-text {
        display: flex;
        flex-direction: column;
      }
      .card__title {
        font: var(--fds-headline-4);
        color: var(--fds-text-high-on-background);
      }
      .card__subtitle {
        font: var(--fds-subtitle-1);
        color: var(--fds-text-high-on-background);
      }
      .card__secondary {
        font: var(--fds-body-2);
        color: var(--fds-text-high-on-background);
        padding: 0 1rem 8px;
      }
    </style>
    <fds-card ?outlined=${outlined} ?fullBleed=${fullBleed}>
      <div slot="primary-action">
        <div class="card__header">
          <div class="card__header-text">
            <div class="card__title">Our Changing Planet</div>
            <div class="card__subtitle">by Kurt Wagner</div>
          </div>
        </div>
        <div class="card__secondary mdc-typography mdc-typography--body2">
          Visit ten places on our planet that are undergoing the biggest changes today.
        </div>
      </div>
      <mwc-button slot="button-action">Read</mwc-button>
      <mwc-button slot="button-action">Bookmark</mwc-button>
      <mwc-icon-button slot="icon-action" icon="favorite"></mwc-icon-button>
      <mwc-icon-button slot="icon-action" icon="share"></mwc-icon-button>
      <mwc-icon-button slot="icon-action" icon="more_vert"></mwc-icon-button>
    </fds-card>`;
};

# Card

Card component

<Canvas>
  <Story
    name="Default"
    args={{ fullBleed: true }}
    parameters={{
      docs: {
        source: {
          code: '<fds-card></fds-card>'
        }
      }
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Installation

```
npm install @finastra/card

```

## API

<ArgsTable of="." story="^" />

## Examples

### Outlined

<Canvas>
  <Story
    name="outlined"
    args={{ outlined: true }}
    parameters={{
      docs: {
        source: {
          code: '<fds-card></fds-card>'
        }
      }
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### FullBleed

<Canvas>
  <Story
    name="fullBleed"
    args={{ fullBleed: true }}
    parameters={{
      docs: {
        source: {
          code: '<fds-card></fds-card>'
        }
      }
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

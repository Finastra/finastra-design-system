import { Meta, Story, Canvas, ArgsTable, Props } from '@web/storybook-prebuilt/addon-docs/blocks.js';
import { html } from 'lit-html';
import { ifDefined } from 'lit-html/directives/if-defined';

import '@finastra/card';

import '@material/mwc-button';

<Meta
  title="Components/Card"
  component={'fds-card'}
  argTypes={{
    outlined: {
      table: {
        category: 'attributes',
        defaultValue: { summary: false }
      },
      control: 'boolean',
      defaultValue: false,
      type: 'boolean',
      description: ''
    },
    fullBleed: {
      table: {
        category: 'attributes',
        defaultValue: { summary: false }
      },
      control: 'boolean',
      defaultValue: false,
      type: 'boolean',
      description: ''
    },
    'icon-action': {
      table: {
        category: 'slot'
      },
      type: 'string',
      description: '',
      control: null
    },
    'button-action': {
      table: {
        category: 'slot'
      },
      type: 'string',
      description: '',
      control: null
    },
    'primary-action': {
      table: {
        category: 'slot'
      },
      type: 'string',
      description: '',
      control: null
    }
  }}
/>

export const StandardCard = ({ outlined = false, fullBleed = false }) => {
  return html` <style>
      .card__header {
        display: flex;
        flex-direction: row;
        padding: 16px;
      }
      .card__header-text {
        display: flex;
        flex-direction: column;
      }
      .card__title,
      .card__subtitle,
      .card__secondary {
        font-family: Roboto, sans-serif;
      }
      .card__title {
        font-size: 1.25rem;
        font-weight: 500;
        line-height: 2rem;
        color: rgba(0, 0, 0);
      }
      .card__subtitle {
        font-size: 0.875rem;
        line-height: 1.375rem;
        font-weight: 500;
        color: rgba(0, 0, 0, 0.54);
        text-decoration: inherit;
        text-decoration-line: inherit;
        text-decoration-style: inherit;
        text-decoration-color: inherit;
      }
      .card__secondary {
        font-size: 0.875rem;
        line-height: 1.25rem;
        font-weight: 400;
        color: rgba(0, 0, 0, 0.54);
        text-decoration: inherit;
        text-transform: inherit;
        padding: 0 1rem 8px;
      }
    </style>
    <fds-card ?outlined=${outlined} ?fullBleed=${fullBleed}>
      <div slot="primary-action">
        <div class="card__header">
          <div class="card__header-text">
            <div class="card__title">Our Changing Planet</div>
            <div class="card__subtitle">by Kurt Wagner</div>
          </div>
        </div>
        <div class="card__secondary mdc-typography mdc-typography--body2">
          Visit ten places on our planet that are undergoing the biggest changes today.
        </div>
      </div>
      <mwc-button slot="button-action">Read</mwc-button>
      <mwc-button slot="button-action">Bookmark</mwc-button>
      <mwc-icon-button slot="icon-action" icon="favorite"></mwc-icon-button>
      <mwc-icon-button slot="icon-action" icon="share"></mwc-icon-button>
      <mwc-icon-button slot="icon-action" icon="more_vert"></mwc-icon-button>
    </fds-card>`;
};

# Card

Card component

<Canvas>
  <Story
    name="Default"
    parameters={{
      docs: {
        source: {
          code: StandardCard({}).strings.toString()
        }
      }
    }}
  >
    {StandardCard({})}
  </Story>
</Canvas>

## Installation

```
npm install @finastra/card

```

## API

<ArgsTable of="." story="^" />

## Examples

### Outlined

<Canvas>
  <Story
    name="outlined"
    parameters={{
      docs: {
        source: {
          code: StandardCard({ outlined: true }).strings.toString()
        }
      }
    }}
  >
    {StandardCard({ outlined: true })}
  </Story>
</Canvas>

### fullBleed

<Canvas>
  <Story
    name="fullBleed"
    parameters={{
      docs: {
        source: {
          code: StandardCard({ fullBleed: true }).strings.toString()
        }
      }
    }}
  >
    {StandardCard({ fullBleed: true })}
  </Story>
</Canvas>

{
  "id": "ThemeBuilder",
  "title": "Theme Builder",
  "description": "Builds the themes",
  "properties": {
    "inputPath": {
      "type": "string",
      "description": "Path to the input file"
    },
    "outputPath": {
      "type": "string",
      "description": "Path to the input file"
    },
    "silent": {
      "type": "boolean",
      "description": "Show output text.",
      "default": false
    },
    "sourceMap": {
      "type": "boolean",
      "description": "Save the sourcemaps."
    },
    "outputStyle": {
      "type": "string",
      "description": "Determines the output format of the final CSS style, (compact | compressed | expanded | nested)"
    },
    "assets": {
      "type": "array",
      "description": "Array of paths to copy",
      "default": [],
      "items": {
        "type": "string",
        "description": "Path to copy"
      }
    }
  }
}

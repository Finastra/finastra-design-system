import { Meta, Story, Canvas, ArgsTable, Props } from '@web/storybook-prebuilt/addon-docs/blocks.js';
import { html } from 'lit';

import '/node_modules/@finastra/<%= fileName %>/dist/src/<%= fileName %>.js';

<Meta
  title="Components/<%= className %>"
  component={'fds-<%= fileName %>'}
  argTypes={{
    name: {
      table: {
        category: 'attributes',
        defaultValue: { summary: '' }
      },
      control: 'text',
      defaultValue: '',
      type: 'text',
      description: 'Somebody'
    },
    '--fds-<%= fileName %>-text-color': {
      table: {
        category: 'css',
        defaultValue: { summary: 'Text color' }
      },
      type: 'string',
      description: 'Text color',
      control: null
    }
  }}
/>

export const Template = ({ name = '' }) => {
  return html`<fds-<%= fileName %>  .name=${name}></fsd-<%= fileName %>>`;
};

# <%= fileName %>

<Canvas>
  <Story
    name="Default"
    parameters={{
      docs: {
        source: {
          code: '<fds-<%= fileName %>></fds-<%= fileName %>>'
        }
      }
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Installation

```
npm install @finastra/<%= fileName %>

```

## API

<ArgsTable of="." story="^" />

## Examples

### Name

<Canvas>
  <Story
    name="Name"
    parameters={{
      docs: {
        source: {
          code: '<fds-<%= fileName %> name="Workd"></fds-<%= fileName %>>'
        }
      }
    }}
  >
    {Template.bind({}, 'World')}
  </Story>
</Canvas>
